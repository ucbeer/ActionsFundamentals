name: Use released hello world docker action
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: ucbeer/hello-world-docker-action@v1
        with:
          who-to-greet: 'Tibike'